% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/environment.R
\name{generate_dockerfile}
\alias{generate_dockerfile}
\title{Generate Dockerfile for Chap model}
\usage{
generate_dockerfile(
  chapkit_version = NULL,
  python_version = "3.12",
  output_path = "Dockerfile",
  lockfile_path = "renv.lock",
  os = "ubuntu",
  os_release = "22.04"
)
}
\arguments{
\item{chapkit_version}{Version of chapkit to install (default: latest)}

\item{python_version}{Python version to install (default: "3.12")}

\item{output_path}{Where to write Dockerfile (default: "Dockerfile")}

\item{lockfile_path}{Path to renv.lock (default: "renv.lock")}

\item{os}{Operating system for system deps (default: "ubuntu")}

\item{os_release}{OS release for system deps (default: "22.04")}
}
\value{
Invisible path to created Dockerfile
}
\description{
Creates a Dockerfile that builds a container with R, renv packages,
Python, and chapkit. The generated image can run the complete service.
}
\examples{
\dontrun{
# Generate with defaults
generate_dockerfile()

# Generate with specific versions
generate_dockerfile(chapkit_version = "0.1.0", python_version = "3.11")
}
}
