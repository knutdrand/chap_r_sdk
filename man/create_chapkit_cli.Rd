% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli.R
\name{create_chapkit_cli}
\alias{create_chapkit_cli}
\title{Create Chapkit-Compatible CLI}
\usage{
create_chapkit_cli(
  train_fn,
  predict_fn,
  model_config_schema = NULL,
  model_info = NULL,
  default_config_path = "config.yml",
  default_model_path = "model.rds",
  args = commandArgs(trailingOnly = TRUE)
)
}
\arguments{
\item{train_fn}{Training function with signature:
\code{function(training_data, model_configuration = list(), run_info = list())} where
\code{training_data} is a tsibble, \code{model_configuration} is a list of user-defined
configuration options, and \code{run_info} is a list containing CHAP-provided run
information (see \code{\link{create_chap_cli}} for details).
Should return a model object that will be automatically saved as RDS.}

\item{predict_fn}{Prediction function with signature:
\code{function(historic_data, future_data, saved_model, model_configuration = list(), run_info = list())}
where all data inputs are tsibbles, \code{saved_model} is a loaded object,
\code{model_configuration} is a list of user-defined configuration options,
and \code{run_info} is a list containing CHAP-provided run information.
Must return a tibble with a \code{samples} list-column containing numeric vectors.}

\item{model_config_schema}{Optional model configuration schema (for info subcommand).}

\item{model_info}{Optional list describing the model's data requirements and capabilities.
See \code{\link{create_chap_cli}} for details on the model_info structure.}

\item{default_config_path}{Default path to config file (default: "config.yml")}

\item{default_model_path}{Default path to model file (default: "model.rds")}

\item{args}{Command line arguments (defaults to \code{commandArgs(trailingOnly = TRUE)})}
}
\value{
Invisible result of the called function
}
\description{
Creates a command-line interface compatible with chapkit's ShellModelRunner.
Uses named arguments (--data, --historic, --future, --output) instead of
positional arguments. Config and model paths have sensible defaults but
can be overridden.
}
\details{
This CLI style is designed for integration with chapkit's ML service framework,
which manages workspaces and file paths automatically.
\subsection{Training Command}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{Rscript model.R train --data <path> [--config <path>] [--model <path>] [--run-info <path>]
}\if{html}{\out{</div>}}
\itemize{
\item \code{--data}: Path to training data CSV (required)
\item \code{--config}: Path to YAML config file (default: config.yml)
\item \code{--model}: Path to save trained model (default: model.rds)
\item \code{--run-info}: Path to run_info YAML/JSON file (optional, provided by CHAP)
}
}

\subsection{Prediction Command}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{Rscript model.R predict --historic <path> --future <path> --output <path> [--config <path>] [--model <path>] [--run-info <path>]
}\if{html}{\out{</div>}}
\itemize{
\item \code{--historic}: Path to historic data CSV (required)
\item \code{--future}: Path to future data CSV (required)
\item \code{--output}: Path to write predictions CSV (required)
\item \code{--config}: Path to YAML config file (default: config.yml)
\item \code{--model}: Path to load trained model (default: model.rds)
\item \code{--run-info}: Path to run_info YAML/JSON file (optional, provided by CHAP)
}
}

\subsection{Info Command}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{Rscript model.R info
}\if{html}{\out{</div>}}
}

\subsection{Chapkit Integration}{

Configure ShellModelRunner in chapkit:

\if{html}{\out{<div class="sourceCode python">}}\preformatted{runner = ShellModelRunner(
    train_command="Rscript model.R train --data \{data_file\} --run-info \{run_info_file\}",
    predict_command="Rscript model.R predict --historic \{historic_file\} --future \{future_file\} --output \{output_file\} --run-info \{run_info_file\}"
)
}\if{html}{\out{</div>}}
}
}
\examples{
\dontrun{
library(chap.r.sdk)

train_fn <- function(training_data, model_configuration = list(), run_info = list()) {
  list(mean = mean(training_data$disease_cases, na.rm = TRUE))
}

predict_fn <- function(historic_data, future_data, saved_model,
                       model_configuration = list(), run_info = list()) {
  future_data |>
    dplyr::mutate(samples = purrr::map(seq_len(dplyr::n()), ~c(saved_model$mean)))
}

if (!interactive()) {
  create_chapkit_cli(train_fn, predict_fn)
}
}
}
