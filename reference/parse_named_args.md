# Parse named arguments from command line

Parses command line arguments in the form –name value or –name=value.

## Usage

``` r
parse_named_args(args, defaults = list())
```

## Arguments

- args:

  Character vector of command line arguments

- defaults:

  Named list of default values for arguments

## Value

Named list of parsed argument values
