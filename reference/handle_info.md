# Handle info subcommand

Internal function that handles the "info" subcommand for
create_chap_cli(). Displays model information and configuration schema
if provided.

## Usage

``` r
handle_info(model_config_schema)
```

## Arguments

- model_config_schema:

  Optional configuration schema to display

## Value

NULL (invisibly)
