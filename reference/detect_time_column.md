# Detect time column in data frame

Searches for common time column names in the data frame. Falls back to
the first column if no standard names are found.

## Usage

``` r
detect_time_column(df)
```

## Arguments

- df:

  Data frame to search

## Value

Name of the detected time column
